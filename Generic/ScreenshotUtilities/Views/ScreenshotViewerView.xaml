<UserControl x:Class="ScreenshotUtilities.Views.ScreenshotViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScreenshotUtilities.Views"
             xmlns:viewModels="clr-namespace:ScreenshotUtilities.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:ScreenshotViewerViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel  Margin="15">
        <ListBox Name="MyItemsControl"
                 DockPanel.Dock="Bottom"
                 ItemsSource="{Binding Screenshots}" 
                 Height="150" 
                 HorizontalAlignment="Stretch"
                 SelectedItem="{Binding SelectedScreenshot}"
                 ScrollViewer.CanContentScroll="True"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.PanningMode="HorizontalOnly"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 VirtualizingStackPanel.VirtualizationMode="Recycling">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding Path}" 
                           RenderOptions.BitmapScalingMode="Fant" 
                           Stretch="Uniform"
                           Margin="2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Image Source="{Binding SelectedScreenshot.Path}"
                   DockPanel.Dock="Top"
                   RenderOptions.BitmapScalingMode="Fant"
                   Stretch="Uniform" />
    </DockPanel>
</UserControl>
