<PluginUserControl x:Class="MetadataUtilities.Controls.PrefixItemControl"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:vm="clr-namespace:MetadataUtilities.ViewModels"
                   d:DataContext="{d:DesignInstance Type=vm:PrefixItemControlViewModel}"
                   mc:Ignorable="d" 
                   d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <TextBlock Text="{Binding Tags.Name}" DockPanel.Dock="Top" />
        <ItemsControl ItemsSource="{Binding Tags.Items}" Margin="0,3,0,3" DockPanel.Dock="Top" >
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <Button DockPanel.Dock="Left" Content="{Binding EditName}" Style="{StaticResource PropertyItemFeedPanelButton}"
                                Command="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=DataContext.SetFeatureFilterCommand}"
                                CommandParameter="{Binding}"/>
                    </DockPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</PluginUserControl>
