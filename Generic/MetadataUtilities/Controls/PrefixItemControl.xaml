<PluginUserControl x:Class="MetadataUtilities.Controls.PrefixItemControl"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:vm="clr-namespace:MetadataUtilities.ViewModels"
                   d:DataContext="{d:DesignInstance Type=vm:PrefixItemControlViewModel}"
                   mc:Ignorable="d" 
                   d:DesignHeight="200" d:DesignWidth="200">
    <DockPanel>
        <ItemsControl ItemsSource="{Binding ItemLists}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" FontFamily="{DynamicResource FontIcoFont}" Style="{DynamicResource Icon}" />
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                        </Label>
                        <ItemsControl Grid.Column="1" ItemsSource="{Binding Items}" Margin="-3,0,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding DisplayName}" Style="{StaticResource PropertyItemFeedPanelButton}"
                                            Command="{Binding RelativeSource={RelativeSource AncestorType=DockPanel}, Path=DataContext.SetFilterCommand}"
                                            CommandParameter="{Binding}"
                                            ToolTip="{Binding DisplayName}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</PluginUserControl>
