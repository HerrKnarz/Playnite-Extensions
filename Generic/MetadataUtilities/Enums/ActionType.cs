namespace MetadataUtilities.Enums
{
    //TODO: Make sure single list items can only be set to one value and use the action set instead of add.
    public enum ActionType
    {
        AddObject,
        RemoveObject,
        ClearField,
        Set,
        None
    }
}
