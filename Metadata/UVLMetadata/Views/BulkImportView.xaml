<UserControl x:Class="UVLMetadata.Views.BulkImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UVLMetadata.Views"
             xmlns:localModels="clr-namespace:UVLMetadata.Models"
             xmlns:knarzHelperControls="clr-namespace:KNARZhelper.Controls"
             xmlns:vm="clr-namespace:UVLMetadata.ViewModels"
             xmlns:c="clr-namespace:KNARZhelper.Controls"
             d:DataContext="{d:DesignInstance Type=vm:BulkImportViewModel}"
             mc:Ignorable="d"
             d:Background="LightGray"
             d:DesignHeight="600"
             d:DesignWidth="1000">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/UVLMetadata;component/Shared/Controls/SettingsStyles.xaml" />
                <ResourceDictionary Source="/UVLMetadata;component/Shared/Controls/CommonStyles.xaml" />
                <d:ResourceDictionary Source="/UVLMetadata;component/Localization/en_US.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Top"
                BorderBrush="{DynamicResource WindowPanelSeparatorBrush}"
                d:BorderBrush="{DynamicResource KNARZDummyBrush}"
                BorderThickness="0,1,0,0"
                Margin="0,0,0,0" />
        <Border DockPanel.Dock="Bottom"
                BorderBrush="{DynamicResource WindowPanelSeparatorBrush}"
                d:BorderBrush="{DynamicResource KNARZDummyBrush}"
                BorderThickness="0,1,0,0"
                Margin="0,0,0,0">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="10">
                <Button Content="{DynamicResource LOCCloseLabel}"
                        d:Content="Close"
                        Command="{Binding CloseCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                        IsDefault="True"
                        Style="{DynamicResource BottomButton}"
                        d:Style="{StaticResource KNARZBottomButton}" />
            </StackPanel>
        </Border>

        <Grid Margin="15, 10"
              DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <c:SettingsSection Heading="{DynamicResource LOCUVLMetadataBulkImportSearchTag}"
                               Grid.Column="0"
                               Margin="0, 0, 15, 0"
                               Width="400">
                <DockPanel>
                    <DockPanel Margin="0, 0, 0, 15"
                               DockPanel.Dock="Top">
                        <Button Content="&#xef00;"
                                Style="{DynamicResource KNARZIconButton}"
                                Click="ClearSearchBox"
                                DockPanel.Dock="Right"
                                Margin="5,0,0,0"
                                Padding="5,5,8,5" />
                        <TextBox Name="SearchBox"
                                 Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                DockPanel.Dock="Bottom"
                                Margin="0, 15, 0, 0">
                        <Button Name="BtnRefreshTags"
                                VerticalAlignment="Center"
                                Margin="0,0,10,0"
                                Width="100"
                                DockPanel.Dock="Right"
                                Content="{DynamicResource LOCUVLMetadataSettingsButtonTagsRefresh}"
                                Command="{Binding RefreshTagsCommand}" />
                        <Button Content="{DynamicResource LOCUVLMetadataBulkImportSearchGames}"
                                Command="{Binding SearchGamesCommand}"
                                IsEnabled="{Binding ElementName=LbTagList, Path=SelectedItems.Count}" />
                    </StackPanel>

                    <ListBox Name="LbTagList"
                             Grid.Column="1"
                             ItemsSource="{Binding TagsViewSource.View}"
                             d:ItemsSource="{d:SampleData}"
                             SelectedItem="{Binding SelectedTag}"
                             SelectionMode="Single"
                             Background="{DynamicResource NormalBrushDark}"
                             d:Background="White"
                             HorizontalContentAlignment="Stretch"
                             VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             ScrollViewer.CanContentScroll="True"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                        <ListBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Margin"
                                                Value="0" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander Margin="0, 0, 0, 4"
                                                              Padding="2"
                                                              Tag="{Binding DataContext.GroupsExpanded, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                              Background="{DynamicResource ExpanderBackgroundBrush}"
                                                              d:Background="White">
                                                        <Expander.Style>
                                                            <Style TargetType="{x:Type Expander}"
                                                                   BasedOn="{StaticResource {x:Type Expander}}">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}"
                                                                                 Value="True">
                                                                        <Setter Property="IsExpanded"
                                                                                Value="True" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Expander.Style>
                                                        <Expander.Header>
                                                            <DockPanel Margin="5"
                                                                       LastChildFill="False">
                                                                <TextBlock FontWeight="Bold"
                                                                           Text="{Binding Name}"
                                                                           FontSize="{StaticResource FontSizeLarger}"
                                                                           d:FontSize="14"
                                                                           VerticalAlignment="Center"
                                                                           Margin="0,0,5,0"
                                                                           DockPanel.Dock="Left"
                                                                           Foreground="{DynamicResource TextBrush}"
                                                                           d:Foreground="Black" />
                                                                <TextBlock FontWeight="Bold"
                                                                           Text="{Binding ItemCount, StringFormat=({0})}"
                                                                           VerticalAlignment="Center"
                                                                           DockPanel.Dock="Right"
                                                                           Foreground="{DynamicResource HighlightGlyphBrush}"
                                                                           d:Foreground="Black" />
                                                            </DockPanel>
                                                        </Expander.Header>
                                                        <Expander.Content>
                                                            <ItemsPresenter />
                                                        </Expander.Content>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Margin"
                                                Value="0" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="True"
                                                              Margin="0, 2, 0, 0"
                                                              Padding="10, 2, 2, 2"
                                                              Background="{DynamicResource ExpanderBackgroundBrush}"
                                                              d:Background="White">
                                                        <Expander.Header>
                                                            <DockPanel Margin="5"
                                                                       LastChildFill="False">
                                                                <TextBlock FontWeight="Bold"
                                                                           Text="{Binding Name}"
                                                                           FontSize="{StaticResource FontSize}"
                                                                           d:FontSize="14"
                                                                           VerticalAlignment="Center"
                                                                           Margin="0,0,5,0"
                                                                           DockPanel.Dock="Left"
                                                                           Foreground="{DynamicResource TextBrush}"
                                                                           d:Foreground="Black" />
                                                                <TextBlock FontWeight="Bold"
                                                                           Text="{Binding ItemCount, StringFormat=({0})}"
                                                                           FontSize="{StaticResource FontSizeSmall}"
                                                                           d:FontSize="10"
                                                                           VerticalAlignment="Center"
                                                                           DockPanel.Dock="Right"
                                                                           Foreground="{DynamicResource HighlightGlyphBrush}"
                                                                           d:Foreground="Black" />
                                                            </DockPanel>
                                                        </Expander.Header>
                                                        <Expander.Content>
                                                            <ItemsPresenter />
                                                        </Expander.Content>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </ListBox.GroupStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="5">
                                    <DockPanel Margin="0, 0, 0, 2">
                                        <TextBlock Text="{Binding GamesCountFormatted}"
                                                   FontSize="{StaticResource FontSizeSmall}"
                                                   d:FontSize="10"
                                                   VerticalAlignment="Center"
                                                   DockPanel.Dock="Right" />
                                        <TextBlock Text="{Binding ShortName}"
                                                   FontWeight="Bold"
                                                   Margin="0,0,5,0"
                                                   TextWrapping="Wrap"
                                                   DockPanel.Dock="Left" />
                                    </DockPanel>
                                    <TextBlock Text="{Binding Description}"
                                               FontSize="{StaticResource FontSizeSmall}"
                                               d:FontSize="10"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </c:SettingsSection>

            <DockPanel Grid.Column="1">
                <c:SettingsSection Heading="{DynamicResource LOCUVLMetadataBulkImportImportAs}"
                                   DockPanel.Dock="Bottom">
                    <DockPanel LastChildFill="False">
                        <ComboBox ItemsSource="{Binding ImportAsModes}"
                                  DisplayMemberPath="Value"
                                  SelectedValuePath="Key"
                                  SelectedValue="{Binding FieldType}"
                                  VerticalAlignment="Center"
                                  MinWidth="50"
                                  DockPanel.Dock="Left" />
                        <TextBox Text="{Binding FieldValue}"
                                 VerticalAlignment="Center"
                                 Margin="10, 0, 0, 0"
                                 MinWidth="100"
                                 DockPanel.Dock="Left" />
                        <Button Content="{DynamicResource LOCUVLMetadataBulkImportSelect}"
                                Margin="5, 0, 0, 0"
                                Command="{Binding SelectFieldValueCommand}"
                                DockPanel.Dock="Left" />

                        <CheckBox Content="{DynamicResource LOCUVLMetadataBulkImportImportLink}"
                                  IsChecked="{Binding ImportLink}"
                                  VerticalAlignment="Center"
                                  Margin="10, 0, 0, 0"
                                  DockPanel.Dock="Left" />
                        <Button Content="{DynamicResource LOCUVLMetadataBulkImportImport}"
                                Command="{Binding ImportTagsCommand}"
                                CommandParameter="{Binding SelectedItems, ElementName=GamesGrid}"
                                Margin="10, 0, 0, 0"
                                IsEnabled="{Binding ElementName=GamesGrid, Path=SelectedItems.Count}"
                                DockPanel.Dock="Right" />
                    </DockPanel>
                </c:SettingsSection>

                <c:SettingsSection Heading="{Binding FoundGamesSectionCaption}"
                                   DockPanel.Dock="Top">

                    <DataGrid Name="GamesGrid"
                              ItemsSource="{Binding MatchedGames}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              ColumnWidth="*"
                              CanUserSortColumns="True"
                              EnableRowVirtualization="True"
                              SelectionMode="Extended">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource LOCGameNameTitle}"
                                                d:Header="Name"
                                                Binding="{Binding PlayniteGame.Game.Name}"
                                                ToolTipService.ToolTip="{Binding PlayniteGame.Game.Name}"
                                                MinWidth="50"
                                                SortDirection="Ascending"
                                                SortMemberPath="PlayniteGame.RealSortingName" />
                            <DataGridTextColumn Header="{DynamicResource LOCGameReleaseYearTitle}"
                                                d:Header="Year"
                                                Binding="{Binding PlayniteGame.Game.ReleaseYear}"
                                                Width="SizeToHeader" />
                            <DataGridTextColumn Header="{DynamicResource LOCPlatformsTitle}"
                                                d:Header="Platforms"
                                                Binding="{Binding PlayniteGame.Platforms}"
                                                ToolTipService.ToolTip="{Binding PlayniteGame.Platforms}"
                                                MinWidth="50" />
                            <DataGridTextColumn Header="{DynamicResource LOCUVLMetadataBulkImportCaptionMatchedName}"
                                                d:Header="Matched name"
                                                Binding="{Binding UVLGame.Name}"
                                                ToolTipService.ToolTip="{Binding UVLGame.Name}"
                                                MinWidth="50" />
                            <DataGridTextColumn Header="{DynamicResource LOCUVLMetadataBulkImportCaptionMatchedRelease}"
                                                d:Header="Matched year"
                                                Binding="{Binding UVLGame.ReleaseDate}"
                                                Width="SizeToHeader" />
                            <DataGridTextColumn Header="{DynamicResource LOCUVLMetadataBulkImportCaptionMatchedPlatform}"
                                                d:Header="Matched platform"
                                                Binding="{Binding UVLGame.PlatformName}" />
                            <DataGridTextColumn Header="{DynamicResource LOCUVLMetadataBulkImportCaptionMatchingType}"
                                                d:Header="Matched by"
                                                Binding="{Binding MatchingTypeCaption}" />

                            <DataGridTextColumn Header="{DynamicResource LOCUVLMetadataBulkImportCaptionMatchingScore}"
                                                d:Header="Matching Score"
                                                Binding="{Binding MatchingScoreCaption}"
                                                Width="SizeToHeader"
                                                SortMemberPath="MatchingScore">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock"
                                           BasedOn="{StaticResource {x:Type TextBlock}}">
                                        <Setter Property="Padding"
                                                Value="4, 2, 2, 2" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding MatchingScore}"
                                                         Value="{x:Static localModels:MatchingScore.Perfect}">
                                                <Setter Property="Background"
                                                        Value="DarkGreen" />
                                                <Setter Property="Foreground"
                                                        Value="LightGreen" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding MatchingScore}"
                                                         Value="{x:Static localModels:MatchingScore.VeryGood}">
                                                <Setter Property="Background"
                                                        Value="DarkGreen" />
                                                <Setter Property="Foreground"
                                                        Value="LightGreen" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding MatchingScore}"
                                                         Value="{x:Static localModels:MatchingScore.Good}">
                                                <Setter Property="Background"
                                                        Value="DarkGoldenrod" />
                                                <Setter Property="Foreground"
                                                        Value="LightGoldenrodYellow" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding MatchingScore}"
                                                         Value="{x:Static localModels:MatchingScore.Acceptable}">
                                                <Setter Property="Background"
                                                        Value="DarkGoldenrod" />
                                                <Setter Property="Foreground"
                                                        Value="LightGoldenrodYellow" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding MatchingScore}"
                                                         Value="{x:Static localModels:MatchingScore.Poor}">
                                                <Setter Property="Background"
                                                        Value="DarkRed" />
                                                <Setter Property="Foreground"
                                                        Value="LightSalmon" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </c:SettingsSection>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>